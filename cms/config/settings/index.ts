import global from './files/global';
import navigation from './files/navigation';
import paths from './files/paths';

const settings = {
  name: 'settings',
  label: 'Settings',
  files: [global, navigation, paths],
};

export default settings;
